export type EnrollmentRequestItem = {
  id: string;
  user_name: string;
  avatar?: string;
  created?: string;
};

export type StudentItem = {
  id: string;
  name: string;
  avatar?: string;
};

export type CourseItem = {
  id: string;
  name: string;
  avatar?: string;
};
