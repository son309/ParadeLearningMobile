export type ProfileInfo = {
  id: string;
  username?: string;
  avatar?: string;
  coverImage?: string;
  description?: string;
  online?: string;
  created?: string;
};

export type ProfileExtras = {
  location?: string;
  link?: string;
};
