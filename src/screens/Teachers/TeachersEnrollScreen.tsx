import React from 'react';
import CourseScreen from '../Course/CourseScreen';

// Reuse CourseScreen's student view to show enrollment-related UI.
export default function TeachersEnrollScreen() {
  return <CourseScreen />;
}
